//defino codigo de servidor o framework

const express = require('express')
const app = express();
// import express from 'express' //Esta sintaxis funciona con REACT y babel (dice que hay que configurar algunas cosas)

module.exports = app;